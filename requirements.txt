# Core runtime (P0/P1)
#
# This file is intentionally small so `pip install -r requirements.txt` works
# on most platforms. Optional features have their own files.

langgraph>=0.2.6
python-dotenv>=1.0.1
langchain>=0.3.8
langchain-core>=0.3.0
langsmith>=0.1.0
langchain-openai>=0.2.9
openai>=1.55.0
tiktoken>=0.8.0
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
tabulate>=0.9.0

# Optional but recommended (P2.27): Pareto frontier plots.
matplotlib>=3.9.0
