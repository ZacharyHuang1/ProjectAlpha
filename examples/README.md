# Examples

This folder contains small sample metadata files for P2.2 features.

- `hard_to_borrow.csv`: instruments that cannot be shorted (list form).
- `borrow_rates.csv`: per-instrument borrow rates (annualized bps).

See `P2_2_IMPLEMENTATION.md` for file formats and CLI usage.
